# QuantumLeap

Landing page for the QuantumLeap Solana timeline arena.

## Run locally

Open `index.html` directly in a browser, or serve the folder with any static server.

## Deploy

The repo is static and Vercel-ready. If the repository is connected to Vercel, pushing to `main` should deploy the landing page.
